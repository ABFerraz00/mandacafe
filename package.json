{
  "name": "restaurante-backend",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "dev": "node index.js",
    "build": "npx prisma generate",
    "db:migrate": "npx prisma migrate dev",
    "db:deploy": "npx prisma migrate deploy",
    "db:seed": "npx prisma db seed",
    "db:studio": "npx prisma studio",
    "db:reset": "npx prisma migrate reset",
    "postinstall": "npx prisma generate",
    "prod:setup": "npm install && npx prisma generate && npx prisma migrate deploy",
    "prod:start": "NODE_ENV=production npm start",
    "docker:build": "docker build -t restaurante-backend .",
    "docker:run": "docker run -p 3000:3000 restaurante-backend",
    "docker:dev": "docker-compose up -d",
    "docker:down": "docker-compose down",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "prisma": {
    "seed": "node prisma/seed.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "@prisma/client": "^6.13.0",
    "body-parser": "^2.2.0",
    "compression": "^1.8.1",
    "cors": "^2.8.5",
    "express": "^4.18.2",
    "express-rate-limit": "^8.0.1",
    "fs-extra": "^11.3.0",
    "helmet": "^8.1.0",
    "morgan": "^1.10.1",
    "pg": "^8.16.3",
    "prisma": "^6.13.0",
    "winston": "^3.17.0"
  },
  "devDependencies": {
    "@types/pg": "^8.15.5"
  }
}
